// const modules = import.meta.glob('./Data/*.json', { eager: true });

// const allJsonFiles = {};

// for (const path in modules) {
//   const key = path.split('/').pop().replace('.json', '');
//   allJsonFiles[key] = modules[path].default; 
// }

// export default allJsonFiles;
